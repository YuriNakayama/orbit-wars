#!/bin/bash
set -euo pipefail

cd "$(dirname "$0")/../backend"

EXIT_CODE=0

echo "== backend: lint =="
uv run ruff check . --fix || EXIT_CODE=1
uv run mypy --config-file pyproject.toml . || EXIT_CODE=1

exit $EXIT_CODE
