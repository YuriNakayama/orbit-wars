#!/bin/bash
# Vast.ai 月次コストレポートを生成する (docs/experiment/vast_cost_report_<YYYY-MM>.md)。
#
# Usage:
#   dev/vast-cost-report [--month YYYY-MM]
set -euo pipefail
cd "$(dirname "$0")/.."
exec uv run --directory backend python -m vast cost-report "$@"
