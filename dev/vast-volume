#!/bin/bash
# Vast.ai persistent volume を管理する.
#
# Usage:
#   dev/vast-volume list
#   dev/vast-volume search [--min-size 15] [--max-dph 0.005]
#   dev/vast-volume create <name> [--size 15]
set -euo pipefail
cd "$(dirname "$0")/.."
exec uv run --directory backend python -m vast volume "$@"
